server {
    listen 80;
    server_name _;

    # Giới hạn upload 50MB (khớp với Nginx ngoài và backend)
    client_max_body_size 50M;

    root /usr/share/nginx/html;
    index index.html;

    # React Router: tất cả route trả về index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy các request ảnh/file upload sang Backend container
    location ^~ /uploads/ {
        proxy_pass http://goldenseafood_backend:3000/uploads/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Static assets: cache dài hạn
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # Tắt log cho favicon và robots
    location = /favicon.ico { log_not_found off; access_log off; }
    location = /robots.txt  { log_not_found off; access_log off; }

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
}
